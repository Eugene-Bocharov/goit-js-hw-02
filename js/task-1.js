'use strict';

const logItems = function (array) {
    // твій код
    for(let value of array){
        let i;
        i++;
       console.log(`${i} - ${value}`);
    }} ;

    logItems(['Mango', 'Poly', 'Ajax', 'Lux', 'Jay', 'Kong']);
